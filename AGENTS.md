# brainstorming — Agent Instructions

এই repo-টা Sojib-এর career plan নিয়ে চিন্তার markdown ফাইল — কোড নয়। প্রতিটা ফাইল একটা বিষয়, ভেতরের প্রতিটা `##` সেকশন একটা প্রশ্নের উত্তর, আর সব মিলে একটাই পরিকল্পনা। [`thought-map.md`](thought-map.md) সেই পরিকল্পনার মানচিত্র: কোন সেকশন কোন ধারণার অংশ, আর কোনটা কোনটার সাথে যুক্ত।

---

## Invariants

- সব response বাংলায়।
- **কোনো অনুমান নয়।** যা ফাইলে বা ব্যবহারকারীর উত্তরে নেই, সেটা লিখবেন না — জিজ্ঞেস করুন, একবারে একটা প্রশ্ন, সাথে নিজের সুপারিশ।
- **সব md ফাইলে একই তথ্য।** দুই জায়গায় দুই রকম সংখ্যা, তারিখ বা সিদ্ধান্ত পেলে **নিজে ঠিক করবেন না** — `thought-map.md`-এর "অমিল" সেকশনে লিখুন আর ব্যবহারকারীকে জিজ্ঞেস করুন। উত্তর পেলে সংশ্লিষ্ট সব ফাইল একসাথে ঠিক করুন।
- সত্যের উৎস = ব্যবহারকারীর নিশ্চিত উত্তর, লেখা থাকে [`ASSUMPTIONS.md`](ASSUMPTIONS.md)-এ — কোনো তথ্য বদলালে আগে ওটা, তারপর এখানকার সংশ্লিষ্ট সব ফাইল।
- **এই repo-র জন্য কোনো টুল, সাইট বা script বানাবেন না।** এখানকার ফাইলগুলো নিজেই বারবার সতর্ক করে: "এবারের কাজ বানানো নয়, পাঠানো।"
- **এক বিষয় = এক ফাইল।** নতুন লেখা আগে কোনো বিষয়ের ফাইলে `##` সেকশন হয়; কোনো বিষয়ে না মিললে তবেই নতুন ফাইল।
- **ফাইলের নামে নম্বর নেই, আর নাম বদলায় না।** তিন DSA, তিন system design, তিন behavioural সাইট আর তিন switch plan এই নামগুলোয় লিংক করে। ক্রম থাকে `thought-map.md` আর `README.md`-এ।

---

## বিষয়ের ফাইলের গড়ন

- `# বিষয়` → এক লাইনের পরিচয় `*কী আছে · Nটা সেকশন*` → সেকশনগুলো, মাঝে `---`।
- প্রতিটা সেকশন: `## প্রশ্ন` → `*উপশিরোনাম · তারিখ*` → `###` উপ-অংশ।
- একই ফাইলের অন্য সেকশনের কথা বললে লিংক নয়, সেকশনের নাম। অন্য ফাইলে লিংক ফাইলে, পাশে সেকশনের নাম। বাংলা `#anchor` নয় — GitHub আর VS Code আলাদা anchor বানায়।
- সেকশন যোগ বা বাদ হলে পরিচয়ের লাইনে সংখ্যা হালনাগাদ।

---

## সেশনের শুরুতে — নতুন লেখা খোঁজা

প্রতি সেশনের শুরুতে (বা ব্যবহারকারী বললে) এই ক্রমে:

1. **স্ক্যান** — repo-র সব `.md` ফাইল আর প্রতিটার `##` সেকশন। **বাদ:** `thought-map.md`, `ASSUMPTIONS.md`, `AGENTS.md`, `CLAUDE.md`, `README.md`, আর `docs/plans/`-এর সবকিছু।
2. **মেলানো** — `thought-map.md`-এর "ফাইল-তালিকা" টেবিলের সাথে। **নতুন** দুই রকম, git-এ commit হোক বা না হোক:
   - তালিকায় নেই এমন `.md` ফাইল;
   - কোনো ফাইলে তালিকার `##` সংখ্যার চেয়ে বেশি সেকশন।

   পুরনো সেকশনে লেখা বদলানো "নতুন" নয়।
3. **কিছু নতুন না থাকলে** — `thought-map.md`-এ হাত দেবেন না। শুধু এক লাইনে জানান: "নতুন লেখা নেই।"
4. **নতুন পেলে** — প্রতিটার জন্য:
   1. পুরোটা পড়ুন।
   2. **বিষয়** — কোন ফাইলের বিষয়ে পড়ে। নতুন ফাইল কোনো বিষয়ে মিললে প্রস্তাব দিন ("এটা `dsa.md`-এ যাবে"); ব্যবহারকারী হ্যাঁ বললে তবেই `##` সেকশন হিসেবে জুড়ুন (শিরোনাম এক ধাপ নিচে) আর আলাদা ফাইলটা মুছুন। কোনো বিষয়ে না মিললে নতুন ফাইল রাখার আগে জিজ্ঞেস করুন — নাম নম্বর ছাড়া kebab-case।
   3. **শাখা** — mindmap-এর ৮টা শাখার কোনটায় যায়। কোনোটায় না মিললে নতুন শাখা বানানোর আগে ব্যবহারকারীকে জিজ্ঞেস করুন।
   4. **লেবেল** দিন — ছোট বাংলা (যেমন "HR call", "সিভি লেখা")।
   5. **যোগসূত্র** — কোন কোন ধারণায় পড়ে (নিচে "যোগসূত্র" টেবিল), দরকার হলে টেবিলে সারি যোগ।
   6. **অমিল** — পুরনো কোনো সেকশনের সংখ্যা, তারিখ বা সিদ্ধান্তের সাথে বিরোধ আছে কিনা। থাকলে "অমিল" সেকশনে লিখে জিজ্ঞেস করুন।
5. **`thought-map.md` হালনাগাদ** — ফাইল-তালিকা, mindmap, যোগসূত্র, অমিল; উপরের "শেষ স্ক্যান" তারিখ, ফাইল আর সেকশনের সংখ্যা। নতুন ফাইল হলে `README.md`-এর "বিষয়" টেবিলেও সারি।
6. **ব্যবহারকারীকে জানান** — কী যোগ হলো, কোন ফাইলে, কী লেবেল, কোন শাখায়, কী অমিল পাওয়া গেল।

**তালিকায় আছে কিন্তু ফাইল বা সেকশনটা আর নেই** — map থেকে সরানোর আগে ব্যবহারকারীকে জিজ্ঞেস করুন।

---

## `thought-map.md`-এর গঠন

ক্রমটা বদলাবেন না:

1. **শিরোনাম-ব্লক** — শেষ স্ক্যানের তারিখ, ফাইল আর সেকশনের সংখ্যা।
2. **Mindmap** — Mermaid `mindmap`।
   - কেন্দ্র: `root((আমার career plan))`
   - ৮টা শাখা: মূল রোগ → প্রথম লক্ষ্য → কোন বাজার → প্রমাণ → আবেদন থেকে offer → সহায়ক প্রস্তুতি → যোগ দেওয়ার পরে → দূরের অঙ্ক
   - পাতা = সেকশনের লেবেল। একটা শাখায় একাধিক ফাইল থাকলে ফাইল ধরে উপ-শাখা (যেমন "সহায়ক প্রস্তুতি › DSA", "দূরের অঙ্ক › আয়ের অঙ্ক")।
   - লেবেলে `( ) [ ] { }` ব্যবহার করবেন না — mindmap এগুলোকে আকৃতি হিসেবে পড়ে আর ভেঙে যায়।
3. **ফাইল-তালিকা** — ফাইল (লিংক) · শাখা · `##` সংখ্যা · সেকশনের লেবেল, career plan-এর ক্রমে। স্ক্যানের রেকর্ড এটাই।
4. **যোগসূত্র** — একই ধারণা যে যে সেকশনে আছে; লেখা সেকশনের লেবেল, লিংক ফাইলে (mindmap আড়াআড়ি তীর আঁকতে পারে না, তাই টেবিল)।
5. **অমিল** — খোলা অমিল উপরে, মীমাংসিতগুলো নিচে তারিখসহ।
