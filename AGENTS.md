# brainstorming — Agent Instructions

এই repo-টা Sojib-এর career plan নিয়ে চিন্তার markdown ফাইল — কোড নয়। প্রতিটা ফাইল একটা প্রশ্নের উত্তর, আর সব মিলে একটাই পরিকল্পনা। [`thought-map.md`](thought-map.md) সেই পরিকল্পনার মানচিত্র: কোন ফাইল কোন ধারণার অংশ, আর কোনটা কোনটার সাথে যুক্ত।

---

## Invariants

- সব response বাংলায়।
- **কোনো অনুমান নয়।** যা ফাইলে বা ব্যবহারকারীর উত্তরে নেই, সেটা লিখবেন না — জিজ্ঞেস করুন, একবারে একটা প্রশ্ন, সাথে নিজের সুপারিশ।
- **সব md ফাইলে একই তথ্য।** দুই ফাইলে দুই রকম সংখ্যা, তারিখ বা সিদ্ধান্ত পেলে **নিজে ঠিক করবেন না** — `thought-map.md`-এর "অমিল" সেকশনে লিখুন আর ব্যবহারকারীকে জিজ্ঞেস করুন। উত্তর পেলে সংশ্লিষ্ট সব ফাইল একসাথে ঠিক করুন।
- সত্যের উৎস = ব্যবহারকারীর নিশ্চিত উত্তর। `legacy_and_wisdom/docs/ASSUMPTIONS.md`-এ একই তথ্য থাকে — কোনো তথ্য বদলালে দুই জায়গাতেই মিলিয়ে দেখুন।
- **এই repo-র জন্য কোনো টুল, সাইট বা script বানাবেন না।** এখানকার ফাইলগুলো নিজেই বারবার সতর্ক করে: "এবারের কাজ বানানো নয়, পাঠানো।"

---

## সেশনের শুরুতে — নতুন ফাইল খোঁজা

প্রতি সেশনের শুরুতে (বা ব্যবহারকারী বললে) এই ক্রমে:

1. **স্ক্যান** — repo-র সব `.md` ফাইল, সাব-ফোল্ডারসহ। **বাদ:** `thought-map.md`, `AGENTS.md`, `CLAUDE.md`, `README.md`, আর `docs/plans/`-এর সবকিছু। `memory.md` গোনা হবে।
2. **মেলানো** — স্ক্যানের তালিকা `thought-map.md`-এর "ফাইল-তালিকা" টেবিলের সাথে। **নতুন ফাইল** = তালিকায় নেই এমন ফাইল, git-এ commit হোক বা না হোক। পুরনো ফাইলে লেখা বদলানো "নতুন" নয়।
3. **কিছু নতুন না থাকলে** — `thought-map.md`-এ হাত দেবেন না। শুধু এক লাইনে জানান: "নতুন ফাইল নেই।"
4. **নতুন ফাইল পেলে** — প্রতিটার জন্য:
   1. পুরোটা পড়ুন।
   2. **শাখা** ঠিক করুন — mindmap-এর ৮টা শাখার কোনটায় যায়। কোনোটায় না মিললে নতুন শাখা বানানোর আগে ব্যবহারকারীকে জিজ্ঞেস করুন।
   3. **লেবেল** দিন — ছোট বাংলা (যেমন "HR call", "সিভি লেখা")।
   4. **যোগসূত্র** — কোন কোন ধারণায় পড়ে (নিচে "যোগসূত্র" টেবিল), দরকার হলে টেবিলে সারি যোগ।
   5. **অমিল** — পুরনো কোনো ফাইলের সংখ্যা, তারিখ বা সিদ্ধান্তের সাথে বিরোধ আছে কিনা। থাকলে "অমিল" সেকশনে লিখে জিজ্ঞেস করুন।
5. **`thought-map.md` হালনাগাদ** — ফাইল-তালিকা, mindmap, যোগসূত্র, অমিল; উপরের "শেষ স্ক্যান" তারিখ ও সংখ্যা।
6. **ব্যবহারকারীকে জানান** — কোন ফাইল যোগ হলো, কী লেবেল, কোন শাখায়, কী অমিল পাওয়া গেল।

**তালিকায় আছে কিন্তু ফাইলটা আর নেই** — map থেকে সরানোর আগে ব্যবহারকারীকে জিজ্ঞেস করুন।

---

## `thought-map.md`-এর গঠন

ক্রমটা বদলাবেন না:

1. **শিরোনাম-ব্লক** — শেষ স্ক্যানের তারিখ, ফাইলের সংখ্যা।
2. **Mindmap** — Mermaid `mindmap`।
   - কেন্দ্র: `root((আমার career plan))`
   - ৮টা শাখা: মূল রোগ → প্রথম লক্ষ্য → কোন বাজার → প্রমাণ → আবেদন থেকে offer → সহায়ক প্রস্তুতি → যোগ দেওয়ার পরে → দূরের অঙ্ক
   - **প্রতি শাখায় সর্বোচ্চ ~৬টা ফাইল।** বেশি হলে উপ-শাখা, ভাগ আসবে ফাইলের নামের prefix বা ফোল্ডার থেকে (যেমন `dsa-prep-*`, `after-getting-response/`)।
   - লেবেলে `( ) [ ] { }` ব্যবহার করবেন না — mindmap এগুলোকে আকৃতি হিসেবে পড়ে আর ভেঙে যায়।
3. **ফাইল-তালিকা** — লেবেল · ফাইল (লিংক) · শাখা। স্ক্যানের রেকর্ড এটাই।
4. **যোগসূত্র** — একই ধারণা যে যে ফাইলে আছে (mindmap আড়াআড়ি তীর আঁকতে পারে না, তাই টেবিল)।
5. **অমিল** — খোলা অমিল উপরে, মীমাংসিতগুলো নিচে তারিখসহ।
